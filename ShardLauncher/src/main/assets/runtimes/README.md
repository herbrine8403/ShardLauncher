# Java 运行时资源

这个目录包含内置的 Java 运行时环境。

## 目录结构

- `jre-8/` - OpenJDK 8 运行时
- `jre-17/` - OpenJDK 17 运行时  
- `jre-21/` - OpenJDK 21 运行时

## 使用说明

运行时会在首次启动时自动提取到应用数据目录中。

用户也可以通过运行库管理界面手动导入 tar.xz 格式的运行时文件。